{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "lldb",
        "request": "launch",
        "name": "Debug",
        "program": "${fileDirname}/${fileBasenameNoExtension}", //"${workspaceFolder}/${fileBasenameNoExtension}",
        "args": [],
        "cwd": "${fileDirname}", //"${workspaceFolder}", 
        "preLaunchTask": "Build with Clang"
      }
    ]
  }